// eslint-disable-next-line no-shadow
export enum Actions {
    DETECT= 'DETECT', JOIN='JOIN', LEAVE='LEAVE'
}

// eslint-disable-next-line no-shadow
export enum VideoActions {
    PLAY= 'play', PAUSE = 'pause', PROGRESS = 'progress'
}
